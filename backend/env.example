# Server Configuration
PORT=8080

# JWT Configuration
JWT_SECRET=your-secret-key-here

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Palette Project Service Configuration
# Required for Palette Project service integration
PALETTE_HOST=https://training.spectrocloud.com
#PALETTE_API_KEY=
# Optional: Specific project UID for scoped access (if not provided, uses tenant scope)
PALETTE_PROJECT_UID=optional-project-uid-for-scoped-access
